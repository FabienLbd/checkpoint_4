{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-flex justify-content-end">
        <a class="btn btn-outline-success mx-1" href="{{ path('price_index') }}">Revenir à la liste</a>
        <button class="btn btn-outline-success mx-1">{{ button_label|default('Valider') }}</button>
    </div>
{{ form_end(form) }}
