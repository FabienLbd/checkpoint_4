{% extends 'base.html.twig' %}

{% block title %}Edit Act{% endblock %}

{% block body %}
<div class="container d-flex justify-content-center mx-auto row act-edit mb-4">
    <div class="col-lg-9">
        <div class="d-flex justify-content-between">
            <h1>Edit Act</h1>
            <div class="d-flex justify-content-between">
                {{ include('act/_delete_form.html.twig') }}
            </div>
        </div>
         {{ include('act/_form.html.twig', {'button_label': 'Editer'}) }}
    </div>
</div>
{% endblock %}
