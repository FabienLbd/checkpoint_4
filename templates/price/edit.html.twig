{% extends 'base.html.twig' %}

{% block title %}Edit Price{% endblock %}

{% block body %}
<div class="container d-flex justify-content-center mx-auto row price-edit mb-4">
    <div class="col-lg-9">
        <div class="d-flex justify-content-between">
        <h1>Edit Price</h1>
        <div>
            {{ include('price/_delete_form.html.twig') }}
        </div>
    </div>
        {{ include('price/_form.html.twig', {'button_label': 'Editer'}) }}
    </div>
</div>
{% endblock %}
